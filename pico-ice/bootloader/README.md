# BOOTlOADER

This directory holds a bootloader.  It is a modified mecrisp-pi gateware.

```0 boot``` reloads the default mecrisp-pi gateware.

```1 boot``` loads a blinky gateware.

```2 boot``` loads an echo server gateware. 
